import React, { useState, createContext, useContext, useEffect } from 'react';
const HeroSection = () => {
    return (
        <div className={`p-4 transition-bg-color duration-slow bg-gray-900`}>
            <div className={`container mx-auto flex justify-between items-center w-11/12 transition-text-color duration-slow `}>
                helllo
            </div>
        </div>
    );
};


export default HeroSection;